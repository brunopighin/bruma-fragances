# Assets — Luxe Parfums

Carpeta para imágenes y recursos del proyecto.

Las imágenes se cargan desde el panel admin (`admin.html`) y se guardan en `localStorage` como base64.

Para producción, reemplazá las imágenes subidas desde admin por URLs externas (CDN, Cloudinary, etc.)
